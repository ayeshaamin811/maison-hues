# Base URL of the Django products API.
# Create React App only exposes variables prefixed with REACT_APP_.
# Copy this file to .env and adjust if your backend runs elsewhere.
#
# Local backend:  cd ../maison-backend && ./env/Scripts/python.exe manage.py runserver
REACT_APP_API_BASE=http://127.0.0.1:8000/api
